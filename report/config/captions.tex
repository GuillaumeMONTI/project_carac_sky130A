% !TeX root = ../main.tex

% ============================================================
% GENERAL CAPTION STYLE
% ============================================================

\captionsetup{
    font=small,
    labelfont={sf,bf,color=AccentColor},
    textfont={color=MutedText},
    justification=raggedright,
    singlelinecheck=false,
    labelsep=quad
}

% ============================================================
% FIGURES
% ============================================================

\captionsetup[figure]{
    position=bottom,
    skip=6pt
}


% ============================================================
% TABLES
% ============================================================

\captionsetup[table]{
    position=top,
    skip=6pt
}

% ============================================================
% FLOAT SPACING
% ============================================================

\setlength{\textfloatsep}{18pt plus 2pt minus 4pt}
\setlength{\floatsep}{14pt plus 2pt minus 2pt}
\setlength{\intextsep}{14pt plus 2pt minus 2pt}
